export * from './mailHelper';
export * from './password.utils';

// export const excludePassword = <T, K extends keyof T>(
//   user: T,
// ): Omit<T, 'password'> => {
//   const { password, ...userWithoutPassword } = user;
//   return userWithoutPassword;
// };
